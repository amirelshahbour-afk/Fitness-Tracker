const STORAGE_KEY='amir-fitness-tracker-v1';
const seed={
  goalWeight:70,
  measurements:[
    {date:'2026-09-04',weight:84.6,bmi:27.6,bfr:24.1,fatMass:20.4,musclePct:58.1,muscleWeight:49.2,water:54.9,bmr:1652,visceralFat:10,subcutaneousFat:25.1,proteinPct:15.4,leanWeight:64.2,proteinMass:13.0},
    {date:'2026-09-11',weight:84.0,bmi:27.4,bfr:24.1,fatMass:20.2,musclePct:57.7,muscleWeight:48.5,water:54.9,bmr:1639,visceralFat:10,subcutaneousFat:25.1,proteinPct:15.4,leanWeight:63.8,proteinMass:12.9}
  ],
  workouts:[
    {date:'2026-09-12',group:'صدر',exercise:'Chest Press',sets:[{w:30,r:10},{w:35,r:8},{w:40,r:6}]},
    {date:'2026-09-12',group:'صدر',exercise:'Incline Chest Press',sets:[{w:25,r:10},{w:30,r:8},{w:35,r:6}]}
  ],
  meals:[
    {date:'2026-09-12',name:'فطور',food:'3 بيض + خبز أسمر + خضار',calories:430,protein:28,carbs:35,fat:18},
    {date:'2026-09-12',name:'غداء',food:'دجاج مشوي + أرز + سلطة',calories:620,protein:52,carbs:68,fat:16}
  ],
  targets:{calories:2100,protein:150,carbs:220,fat:65}
};
let data=load();
let deferredPrompt=null;
function load(){try{return JSON.parse(localStorage.getItem(STORAGE_KEY))||structuredClone(seed)}catch{return structuredClone(seed)}}
function save(){localStorage.setItem(STORAGE_KEY,JSON.stringify(data));renderAll()}
function fmtDate(d){return new Intl.DateTimeFormat('ar-SA',{day:'numeric',month:'long',year:'numeric'}).format(new Date(d+'T12:00:00'))}
function latest(){return [...data.measurements].sort((a,b)=>a.date.localeCompare(b.date)).at(-1)}
function previous(){const a=[...data.measurements].sort((x,y)=>x.date.localeCompare(y.date));return a.length>1?a.at(-2):null}
function delta(key,digits=1){const l=latest(),p=previous();if(!l||!p||l[key]==null||p[key]==null)return '—';const d=l[key]-p[key];return `${d>0?'+':''}${d.toFixed(digits)}`}
function renderDashboard(){const l=latest();if(!l)return;document.querySelector('#heroWeight').textContent=`${l.weight.toFixed(1)} كجم`;document.querySelector('#heroDate').textContent=fmtDate(l.date);const start=data.measurements[0]?.weight||l.weight;const total=Math.max(.1,start-data.goalWeight),done=Math.max(0,start-l.weight);const pct=Math.max(0,Math.min(100,Math.round(done/total*100)));document.querySelector('#goalProgress').textContent=`${pct}%`;
 const metrics=[['BMI',l.bmi,delta('bmi')],['دهون الجسم',`${l.bfr}%`,`${delta('bfr')}%`],['كتلة الدهون',`${l.fatMass} كجم`,`${delta('fatMass')} كجم`],['وزن العضلات',`${l.muscleWeight} كجم`,`${delta('muscleWeight')} كجم`],['الماء',`${l.water}%`,`${delta('water')}%`],['BMR',`${l.bmr} kcal`,`${delta('bmr',0)} kcal`],['دهون حشوية',l.visceralFat,delta('visceralFat',0)],['وزن دون دهون',`${l.leanWeight} كجم`,`${delta('leanWeight')} كجم`]];
 document.querySelector('#metricCards').innerHTML=metrics.map(m=>`<div class="metric"><div class="label">${m[0]}</div><div class="value">${m[1]}</div><div class="trend">التغير: ${m[2]}</div></div>`).join('');drawChart();
 const today=new Date().toISOString().slice(0,10);const tw=data.workouts.filter(x=>x.date===today);document.querySelector('#todayWorkout').innerHTML=tw.length?tw.map(w=>`<div class="stack-item"><h4>${w.exercise}</h4><div class="chips">${w.sets.map((s,i)=>`<span class="chip">${i+1}: ${s.w} كجم × ${s.r}</span>`).join('')}</div></div>`).join(''):`<div class="empty">لا يوجد تمرين مسجل اليوم.</div>`;
 const tm=data.meals.filter(x=>x.date===today);const sums=sumMeals(tm);document.querySelector('#nutritionSummary').innerHTML=`<div class="chips"><span class="chip">${sums.calories} سعرة</span><span class="chip">بروتين ${sums.protein}غ</span><span class="chip">كارب ${sums.carbs}غ</span><span class="chip">دهون ${sums.fat}غ</span></div>`}
function drawChart(){const c=document.querySelector('#weightChart'),ctx=c.getContext('2d'),arr=[...data.measurements].sort((a,b)=>a.date.localeCompare(b.date));ctx.clearRect(0,0,c.width,c.height);if(arr.length<2)return;const pad=45,min=Math.min(...arr.map(x=>x.weight))-1,max=Math.max(...arr.map(x=>x.weight))+1;ctx.strokeStyle='#cbd5e1';ctx.lineWidth=1;for(let i=0;i<4;i++){const y=pad+i*(c.height-2*pad)/3;ctx.beginPath();ctx.moveTo(pad,y);ctx.lineTo(c.width-pad,y);ctx.stroke()}ctx.strokeStyle='#0ea5a8';ctx.lineWidth=5;ctx.lineJoin='round';ctx.beginPath();arr.forEach((m,i)=>{const x=pad+i*(c.width-2*pad)/(arr.length-1);const y=c.height-pad-(m.weight-min)/(max-min)*(c.height-2*pad);i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.stroke();arr.forEach((m,i)=>{const x=pad+i*(c.width-2*pad)/(arr.length-1);const y=c.height-pad-(m.weight-min)/(max-min)*(c.height-2*pad);ctx.fillStyle='#0f172a';ctx.beginPath();ctx.arc(x,y,6,0,Math.PI*2);ctx.fill();ctx.font='24px sans-serif';ctx.fillText(String(m.weight),x-22,y-14)})}
function renderWorkouts(){const list=document.querySelector('#workoutList');const arr=[...data.workouts].sort((a,b)=>b.date.localeCompare(a.date));list.innerHTML=arr.length?arr.map((w,i)=>`<div class="stack-item"><div class="stack-item-head"><div><h4>${w.exercise}</h4><p class="muted">${w.group} · ${fmtDate(w.date)}</p></div><button class="secondary" onclick="removeWorkout(${i})">حذف</button></div><div class="chips">${w.sets.map((s,n)=>`<span class="chip">مجموعة ${n+1}: ${s.w} كجم × ${s.r}</span>`).join('')}</div></div>`).join(''):`<div class="empty">ابدأ بإضافة أول تمرين.</div>`}
function sumMeals(arr){return arr.reduce((a,m)=>({calories:a.calories+(+m.calories||0),protein:a.protein+(+m.protein||0),carbs:a.carbs+(+m.carbs||0),fat:a.fat+(+m.fat||0)}),{calories:0,protein:0,carbs:0,fat:0})}
function renderNutrition(){const today=new Date().toISOString().slice(0,10),arr=data.meals.filter(m=>m.date===today),s=sumMeals(arr),items=[['السعرات',s.calories,data.targets.calories,'kcal'],['البروتين',s.protein,data.targets.protein,'غ'],['الكربوهيدرات',s.carbs,data.targets.carbs,'غ'],['الدهون',s.fat,data.targets.fat,'غ']];document.querySelector('#macroGrid').innerHTML=items.map(x=>`<div class="macro"><span class="muted">${x[0]}</span><strong>${x[1]} / ${x[2]} ${x[3]}</strong><div class="progress"><span style="width:${Math.min(100,Math.round(x[1]/x[2]*100))}%"></span></div></div>`).join('');document.querySelector('#mealList').innerHTML=arr.length?arr.map((m,i)=>`<div class="stack-item"><div class="stack-item-head"><div><h4>${m.name}</h4><p class="muted">${m.food}</p></div><button class="secondary" onclick="removeMeal(${i})">حذف</button></div><div class="chips"><span class="chip">${m.calories} سعرة</span><span class="chip">P ${m.protein}غ</span><span class="chip">C ${m.carbs}غ</span><span class="chip">F ${m.fat}غ</span></div></div>`).join(''):`<div class="empty">لم تسجل وجبات اليوم بعد.</div>`}
function renderMeasurements(){const arr=[...data.measurements].sort((a,b)=>b.date.localeCompare(a.date));document.querySelector('#measurementList').innerHTML=arr.map((m,i)=>`<div class="stack-item"><div class="stack-item-head"><div><h4>${m.weight} كجم</h4><p class="muted">${fmtDate(m.date)}</p></div><button class="secondary" onclick="removeMeasurement(${i})">حذف</button></div><div class="chips"><span class="chip">BMI ${m.bmi}</span><span class="chip">دهون ${m.bfr}%</span><span class="chip">عضلات ${m.muscleWeight} كجم</span><span class="chip">ماء ${m.water}%</span><span class="chip">BMR ${m.bmr}</span></div></div>`).join('')}
function renderAll(){renderDashboard();renderWorkouts();renderNutrition();renderMeasurements()}
function fieldsHtml(fields){return fields.map(f=>`<div class="field ${f.full?'full':''}"><label>${f.label}</label><input name="${f.name}" type="${f.type||'text'}" step="${f.step||'any'}" value="${f.value??''}" required></div>`).join('')}
function openModal(type){const modal=document.querySelector('#modal'),fields=document.querySelector('#modalFields');modal.dataset.type=type;if(type==='measurement'){document.querySelector('#modalTitle').textContent='قياس جديد';fields.innerHTML=fieldsHtml([{label:'التاريخ',name:'date',type:'date',value:new Date().toISOString().slice(0,10)},{label:'الوزن (كجم)',name:'weight',type:'number'},{label:'BMI',name:'bmi',type:'number'},{label:'دهون الجسم %',name:'bfr',type:'number'},{label:'كتلة الدهون كجم',name:'fatMass',type:'number'},{label:'وزن العضلات كجم',name:'muscleWeight',type:'number'},{label:'الماء %',name:'water',type:'number'},{label:'BMR',name:'bmr',type:'number'},{label:'دهون حشوية',name:'visceralFat',type:'number'},{label:'وزن دون دهون',name:'leanWeight',type:'number'}])}else if(type==='meal'){document.querySelector('#modalTitle').textContent='إضافة وجبة';fields.innerHTML=fieldsHtml([{label:'التاريخ',name:'date',type:'date',value:new Date().toISOString().slice(0,10)},{label:'اسم الوجبة',name:'name'},{label:'مكونات الوجبة',name:'food',full:true},{label:'السعرات',name:'calories',type:'number'},{label:'البروتين (غ)',name:'protein',type:'number'},{label:'الكربوهيدرات (غ)',name:'carbs',type:'number'},{label:'الدهون (غ)',name:'fat',type:'number'}])}else{document.querySelector('#modalTitle').textContent='تمرين جديد';fields.innerHTML=fieldsHtml([{label:'التاريخ',name:'date',type:'date',value:new Date().toISOString().slice(0,10)},{label:'المجموعة العضلية',name:'group'},{label:'التمرين',name:'exercise',full:true},{label:'مجموعة 1 وزن',name:'w1',type:'number'},{label:'عدات',name:'r1',type:'number'},{label:'مجموعة 2 وزن',name:'w2',type:'number'},{label:'عدات',name:'r2',type:'number'},{label:'مجموعة 3 وزن',name:'w3',type:'number'},{label:'عدات',name:'r3',type:'number'}])}modal.showModal()}
window.removeWorkout=i=>{const arr=[...data.workouts].sort((a,b)=>b.date.localeCompare(a.date));const target=arr[i];data.workouts=data.workouts.filter(x=>x!==target);save()};window.removeMeal=i=>{const today=new Date().toISOString().slice(0,10),arr=data.meals.filter(m=>m.date===today);data.meals=data.meals.filter(x=>x!==arr[i]);save()};window.removeMeasurement=i=>{if(data.measurements.length<=1)return alert('احتفظ بقياس واحد على الأقل');const arr=[...data.measurements].sort((a,b)=>b.date.localeCompare(a.date));data.measurements=data.measurements.filter(x=>x!==arr[i]);save()};
document.querySelectorAll('.tab').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.tab,.panel').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelector('#'+b.dataset.tab).classList.add('active')}));
document.querySelector('#newWorkoutBtn').onclick=()=>openModal('workout');document.querySelector('#newMealBtn').onclick=()=>openModal('meal');document.querySelector('#newMeasurementBtn').onclick=()=>openModal('measurement');
document.querySelector('#modalForm').addEventListener('submit',e=>{e.preventDefault();const fd=Object.fromEntries(new FormData(e.target).entries()),type=document.querySelector('#modal').dataset.type;if(type==='measurement'){['weight','bmi','bfr','fatMass','muscleWeight','water','bmr','visceralFat','leanWeight'].forEach(k=>fd[k]=+fd[k]);data.measurements.push(fd)}else if(type==='meal'){['calories','protein','carbs','fat'].forEach(k=>fd[k]=+fd[k]);data.meals.push(fd)}else{data.workouts.push({date:fd.date,group:fd.group,exercise:fd.exercise,sets:[{w:+fd.w1,r:+fd.r1},{w:+fd.w2,r:+fd.r2},{w:+fd.w3,r:+fd.r3}]})}document.querySelector('#modal').close();e.target.reset();save()});
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;document.querySelector('#installBtn').classList.remove('hidden')});document.querySelector('#installBtn').onclick=async()=>{if(deferredPrompt){deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null}};
if('serviceWorker'in navigator)navigator.serviceWorker.register('sw.js');renderAll();
